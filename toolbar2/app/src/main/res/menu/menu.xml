<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Menus que si pueden aparecen como botones -->
    <item android:id="@+id/action_search"
        android:icon="@drawable/ic_find_in_page"
        android:title="@string/app_name"
        app:showAsAction="ifRoom" />


    <item android:id="@+id/add_phrase"
        android:icon="@drawable/ic_book"
        android:title="@string/nuevo_libro"
        app:showAsAction="ifRoom" />

    <!--menus que nunca apareceran como iconos aunque haya sitio -->
    <item android:id="@+id/action_settings"
        android:title="@string/app_name"
        app:showAsAction="never" />

   <!-- menu con submenú -->
    <item android:title="Libros">
        <menu>
            <item
                android:id="@+id/menu_nuevo_libro"
                android:icon="@drawable/ic_book"
                android:title="Nuevo libro" />
            <item
                android:id="@+id/menu_busqueda_titulo"
                android:icon="@drawable/ic_find_in_page"
                android:title="Búsqueda por título" />
        </menu>
    </item>

    <item
        android:id="@+id/menu_recargar"
        android:title="Recargar" />

    <item
        android:id="@+id/menu_configuracion"
        android:title="Configuración" />

    <item
        android:id="@+id/menu_about"
        android:title="Sobre Toolbar2" />
</menu>